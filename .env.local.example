# ============================================
# POCKETBASE
# ============================================
# PocketBase server URL (server-side, used by Next.js server components/actions)
# - Local dev: http://127.0.0.1:8080
# - Production (Docker): http://pocketbase:8080 (use service name)
POCKETBASE_URL=http://127.0.0.1:8080

# Client-side PocketBase URL (exposed to browser via Next.js rewrite proxy)
# This is only used as fallback - the app uses /api/pocketbase proxy by default
NEXT_PUBLIC_POCKETBASE_URL=http://127.0.0.1:8080

# Admin token for admin operations (get from: pocketbase superusers list)
POCKETBASE_ADMIN_TOKEN=your-admin-jwt-token

# PocketBase typegen (for generating TypeScript types)
PB_TYPEGEN_URL=http://127.0.0.1:8080
PB_TYPEGEN_EMAIL=admin@myproject.com
PB_TYPEGEN_PASSWORD=secr3tp@ssword!


# ============================================
# CLOUDFLARE R2 (S3-compatible)
# ============================================
# R2 Endpoint (e.g., https://<account-id>.r2.cloudflarestorage.com)
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# R2 Credentials
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key

# R2 Bucket name
R2_BUCKET_NAME=your-bucket-name

# Optional: Public URL for the bucket (for public files)
# R2_PUBLIC_URL=https://pub-your-hash.r2.dev

# ============================================
# PRODUCTION NOTES
# ============================================
# For production with Docker Compose:
# 1. Set POCKETBASE_URL=http://pocketbase:8080 (service name, not localhost)
# 2. Set NEXT_PUBLIC_POCKETBASE_URL to your public domain
# 3. Configure R2 bucket with proper CORS rules
# 4. Use HTTPS for all public-facing URLs
#
# Example production values:
# POCKETBASE_URL=http://pocketbase:8080          # Internal Docker network
# NEXT_PUBLIC_POCKETBASE_URL=https://api.yourdomain.com
