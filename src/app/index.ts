/**
 * App Layer - Public API
 *
 * This is the highest FSD layer. It contains:
 * - Global providers (React Query, Theme, Auth, etc.)
 * - Global styles initialization
 * - App-wide configuration
 *
 * The app layer can import from all other FSD layers.
 */

export { Providers } from "./providers";
